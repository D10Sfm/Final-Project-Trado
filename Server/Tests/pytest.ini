[pytest]
addopts= -rA -v -s

markers =
        smoke: this is smoke scenario
        regression: this is regression scenario
        sanity: this is sanity scenario
        server: this is server-side(api) scenario
        serverlogin: this is server-side(api) scenario on login component
        db: this is DB scenario
